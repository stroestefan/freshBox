# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# Comented out for now because these came from the uni PC and I assume I don't have the same stuff here
# # User specific environment and startup programs
# PATH=$PATH:/opt/teaching/bin:/opt/common/bin:/opt/scripts
# PATH=$PATH:$HOME/bin

# export PATH
# case $(tty) in
# /dev/tty1) exec /opt/teaching/bin/csstartx
# esac
